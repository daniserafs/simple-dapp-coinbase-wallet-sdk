# Simple DApp testing Coinbase Wallet SDK

## Running on your local machine
  create .env.local file, follow the .env.example to add your own INFURA PROJECT ID

  on terminal `yarn install` after `yarn dev`
